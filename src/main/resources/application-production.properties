# Configuración para producción (Render) - MovimientoService
spring.application.name=MovimientoService

# Configuración del servidor
server.port=${PORT:8090}
server.address=0.0.0.0

# Configuración PostgreSQL para Render (usando variables de entorno)
spring.datasource.url=${DATABASE_URL:postgresql://movimientodb_user:RMOLSFc9KrxqbdWF1NAp61DubkGIDmpH@dpg-d3133vumcj7s73808pt0-a/movimientodb}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# URLs de otros microservicios en Render
usuariosservice.url=${USUARIOS_SERVICE_URL:http://localhost:8083}
productosservice.url=${PRODUCTOS_SERVICE_URL:http://localhost:8084}
stockservice.url=${STOCK_SERVICE_URL:http://localhost:8081}

# Endpoints de actuator para health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Configuración de logging
logging.level.root=INFO
logging.level.com.inventario=INFO
