# Configuración para producción (Render) - MovimientoService
spring.application.name=MovimientoService

# Configuración del servidor
server.port=${PORT:8090}
server.address=0.0.0.0

# Configuración PostgreSQL para Render (usando variables de entorno separadas)
spring.datasource.url=jdbc:postgresql://dpg-d3133vumcj7s73808pt0-a.oregon-postgres.render.com:5432/movimientodb
spring.datasource.username=${DB_USERNAME:movimientodb_user}
spring.datasource.password=${DB_PASSWORD:RMOLSFc9KrxqbdWF1NAp61DubkGIDmpH}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# URLs de otros microservicios en Render
usuariosservice.url=${USUARIOS_SERVICE_URL:https://usuariosservice.onrender.com}
productosservice.url=${PRODUCTOS_SERVICE_URL:https://productosservices.onrender.com}
stockservice.url=${STOCK_SERVICE_URL:https://stockservice-wki5.onrender.com}

# Endpoints de actuator para health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Configuración de logging
logging.level.root=INFO
logging.level.com.inventario=INFO
