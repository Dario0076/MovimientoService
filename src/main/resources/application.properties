# Exponer endpoint health
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
spring.application.name=MovimientoService

# Configuración de base de datos PostgreSQL para Render
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d3133vumcj7s73808pt0-a.oregon-postgres.render.com:5432/movimientodb}
spring.datasource.username=${DB_USERNAME:movimientouser}  
spring.datasource.password=${DB_PASSWORD:EG6GNfwlPd3rG2p7LnTHtHZPChv9z1lL}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never
spring.jpa.show-sql=false

server.port=8090

# URLs de otros microservicios en Render
usuariosservice.url=${USUARIOS_SERVICE_URL:https://usuariosservice.onrender.com}
productosservice.url=${PRODUCTOS_SERVICE_URL:https://productosservices.onrender.com}
stockservice.url=${STOCK_SERVICE_URL:https://stockservice-wki5.onrender.com}
